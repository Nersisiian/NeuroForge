[package]
name = "neuroforge-immortal"
version = "0.1.0"
edition = "2021"
description = "Immortal state using CRDT (Automerge)"

[dependencies]
automerge = "0.5"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tokio = { version = "1", features = ["full"] }
