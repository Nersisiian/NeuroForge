import React from 'react';
import ReactDOM from 'react-dom/client';
import FluidCanvas from './App';

ReactDOM.createRoot(document.getElementById('root')!).render(
    <React.StrictMode>
        <FluidCanvas />
    </React.StrictMode>
);
